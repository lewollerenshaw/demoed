import { StyleSheet } from 'react-native';

const mediaplayerStyles = StyleSheet.create({
  mediaActions: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    paddingHorizontal: 10,
    marginBottom: 10,
  },
  itemActions: {
    marginTop: 10,
    marginBottom: 4,
    flexDirection: 'row',
    justifyContent: 'space-between',
    paddingHorizontal: 10,
  },
});

export default mediaplayerStyles;
